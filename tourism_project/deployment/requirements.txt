# streamlit is provided by the Streamlit Space SDK (via sdk_version in README),
# so it is intentionally NOT pinned here. These are the model's runtime deps;
# scikit-learn / xgboost MUST match the training versions so the pickle loads.
pandas==2.2.2
huggingface_hub==0.32.6
joblib==1.5.1
scikit-learn==1.6.0
xgboost==2.1.4
